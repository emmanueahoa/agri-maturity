version: '3.8'
services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - ./db:/usr/src/app/db
      - ./public:/usr/src/app/public
    environment:
      - NODE_ENV=development
      - SESSION_SECRET=devsecret
    command: npm run dev
